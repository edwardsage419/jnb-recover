# Repository Discoverability Setup

## English

This file records repository settings that cannot currently be changed through the connected automation interface.

### Recommended repository description

`Offline, read-only recovery of scientific worksheet data from SigmaPlot .JNB files to CSV and JSON.`

### Recommended topics

`sigmaplot`, `jnb`, `data-recovery`, `scientific-data`, `scientific-software`, `data-extraction`, `data-conversion`, `interoperability`, `legacy-data`, `csv`, `json`, `python`, `ios`, `iphone`, `offline`

Topics should stay specific to the current product and evidence. Avoid unrelated high-traffic topics solely for visibility.

### Recommended labels

Custom labels for future creation:

* `compatibility` — compatibility reports and unsupported JNB variants
* `scientific-validation` — evidence, cross-checks, and scientific verification
* `needs-fixture` — issue cannot safely change parser behavior without a reproducible fixture
* `unsupported-format` — observed content is outside the currently proven JNB record families
* `scientific-risk` — issue may affect interpretation of recovered scientific data

Useful standard GitHub labels include `bug`, `enhancement`, `documentation`, `good first issue`, `help wanted`, and `question`.

### Social preview

Recommended size: 1280 × 640 PNG with a solid background. Keep the visual simple and include the project name, the phrase `Recover SigmaPlot JNB scientific data`, and the open output formats `CSV + JSON`.

### Exposure principles

Use precise search terms in README, release notes, repository topics, and external references. Prefer terms that describe an actual user problem: SigmaPlot JNB recovery, legacy scientific data recovery, worksheet extraction, CSV conversion, and scientific interoperability.

Do not overstate format coverage or claim a complete JNB specification. Scientific credibility is part of discoverability for this project.

---

## 简体中文

本文件记录目前无法通过已连接自动化接口直接修改的 GitHub 仓库设置。

### 建议的仓库描述

`Offline, read-only recovery of scientific worksheet data from SigmaPlot .JNB files to CSV and JSON.`

这句话建议保留英文，以便 GitHub 和外部搜索更容易匹配 SigmaPlot、JNB、CSV、JSON 等核心关键词。

### 建议 Topics

`sigmaplot`、`jnb`、`data-recovery`、`scientific-data`、`scientific-software`、`data-extraction`、`data-conversion`、`interoperability`、`legacy-data`、`csv`、`json`、`python`、`ios`、`iphone`、`offline`

Topics 应保持与当前产品能力和证据直接相关。不要为了流量加入与项目无关的热门 Topic。

### 建议 Labels

后续建议建立的自定义标签：

* `compatibility`：兼容性报告和未支持 JNB 变体
* `scientific-validation`：证据、交叉比对和科学验证
* `needs-fixture`：缺少可复现 fixture，因此不能安全修改解析器行为
* `unsupported-format`：观察到的内容超出当前已有证据支持的 JNB record family
* `scientific-risk`：问题可能影响恢复科研数据的解释

同时可以使用 GitHub 常见标准标签：`bug`、`enhancement`、`documentation`、`good first issue`、`help wanted`、`question`。

### Social Preview

建议使用 1280 × 640 PNG，采用实色背景。视觉保持简单，包含项目名、`Recover SigmaPlot JNB scientific data` 和开放输出格式 `CSV + JSON`。

### 曝光原则

README、Release Notes、Repository Topics 和外部介绍中应使用精确搜索词。优先描述真实用户问题，例如 SigmaPlot JNB recovery、legacy scientific data recovery、worksheet extraction、CSV conversion 和 scientific interoperability。

不要夸大格式覆盖率，也不要声称已经掌握完整 JNB specification。对于本项目，科学可信度本身也是提高传播和采用概率的重要条件。
